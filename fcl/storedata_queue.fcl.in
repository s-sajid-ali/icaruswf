process_name : DetSim

  outputs : {
      hepnos : {
         module_type: LoadbalancingOutput
         fileName: "icarus"
	 async: "${ENABLE_HEPNOS_ASYNC}"
	 batch: "${ENABLE_HEPNOS_BATCH}"
      }
  }

services.HepnosDataStore: {
   protocol: "${MERCURY_TRANSPORT_PROTOCOL}"
}

physics : {
 end_path : [hepnos]
 }
